namespace Dominio.Entidades
{
    public enum TipoDeUsuario
    {
        Administrador,
        Miembro
    }

    public enum EstadoUsuario
    {
        Activo,
        Inactivo,
        Bloqueado
    }
}
